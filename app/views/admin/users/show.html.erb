会員情報詳細

<div class="container">
  <div class="row">
    <div class="col">
      <table class="table">
        <tr>
          <td>名前</td>
          <td><%= @user.name %></td>
        </tr>
        <tr>
          <td>メールアドレス</td>
          <td><%= @user.email %></td>
        </tr>
      </table>
      <%= image_tag @user.get_image(200,200) %>
    </div>
  </div>
  <div class="row">
    <div class="col-2">
      <%= link_to "編集する", edit_admin_user_path(@user.id), class: "btn btn-primary" %>
      <!-- ブックマーク一覧 -->
    </div>
    <div class="col-2">
      <%= link_to "投稿を見る", review_list_path, class: "btn btn-primary" %>
    </div>
  </div>
  <div class="col">
    
  </div>
</div>